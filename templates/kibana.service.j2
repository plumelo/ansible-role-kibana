[Unit]
Description=Kibana
After=network.target

[Service]
User={{ kibana_user }}
WorkingDirectory={{ kibana_path }}
ExecStart={{ kibana_path }}/bin/kibana -c {{kibana_path}}/config/kibana.yml
Type=simple
PIDFile=/var/run/kibana.pid
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
